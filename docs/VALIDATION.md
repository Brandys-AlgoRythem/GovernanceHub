# Validation

## Sprint 1 validation

This sprint uses a static site architecture.

## Checks completed

- Root `index.html` exists
- Root `index.html` links to CSS files in `styles/`
- Internal pages link to CSS with `../styles/`
- Landing page links to `pages/dashboard.html`
- Dashboard navigation links to existing starter pages
- README documents the current stack and local run process
- `.nojekyll` exists for GitHub Pages compatibility

## Build notes

There is no package installation or build command because the project currently has no package manager, framework, or dependency graph.

## Local run

Open `index.html` directly in a browser.
